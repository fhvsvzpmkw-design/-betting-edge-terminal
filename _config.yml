# Public GitHub Pages build policy.
# Meat Desk source documents are private research inputs. The public UI reads
# only the source/status manifest and must never publish the original PDFs.
exclude:
  - research/season-previews/source-pdfs
