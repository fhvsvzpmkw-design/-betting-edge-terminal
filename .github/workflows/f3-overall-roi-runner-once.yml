name: F3 overall ROI one-time

on:
  push:
    branches: [main]
    paths:
      - 'tools/f3-overall-roi-once.mjs'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply overall ROI
        run: node tools/f3-overall-roi-once.mjs
      - name: Commit F3 ROI update
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add index.html
          git commit -m 'Show overall ROI in F3 summary'
          git push origin HEAD:main
