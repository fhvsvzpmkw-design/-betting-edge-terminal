name: VigScope Heat step 1 calibration

on:
  workflow_dispatch:
  push:
    paths:
      - 'tools/vigscope-heat-step1-calibration.mjs'
      - '.github/workflows/vigscope-heat-step1-calibration.yml'

jobs:
  calibrate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Calibrate Heat confidence weighting
        env:
          CALIBRATION_START: '2026-08-15'
          CALIBRATION_END: '2026-09-01'
        run: node tools/vigscope-heat-step1-calibration.mjs
