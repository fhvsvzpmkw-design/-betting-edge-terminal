name: Lou asset pack check once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/lou-asset-pack-check-once.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Lou Vega asset pack
        run: node tests/lou-vega-v2.test.mjs
