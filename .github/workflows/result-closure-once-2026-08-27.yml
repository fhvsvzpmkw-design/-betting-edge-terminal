name: Archived one-off Betting Edge result closure 2026-08-27

on:
  workflow_dispatch:

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived one-off result closure runner; intentionally disabled."
