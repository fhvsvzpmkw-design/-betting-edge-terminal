# Forecast attribution and retrieval repair — September 21

## Changes

- Fixed the results-index reader/writer mismatch. Reviewed/context forecast IDs and explicitly adopted fair-input IDs are separate, including selection timelines. The legacy forecastRecordIds field is a navigation union.
- Connected existing forecast fallback queues to the event research plan, grouped by event/source. The producer can see the next sources without a separate coverage dump. A queued source is never reported as checked.
- Added a draft-only capture importer. It validates event/market identity, timestamps and current applicability; preserves immutable records; rejects conflicting IDs and forged FOUND bindings; does not select a fair, status or stake.
- Updated shared Main authority with the import/revalidation procedure and the real, dated recovery records. The five task schedules, odds budget, Core thresholds and Graham arithmetic are unchanged.

## Actual verification

The full local results rebuild retained 3,353 card appearances. It restored forecast navigation links on 141 appearances; two appearances explicitly reference adopted forecasts. Card grades, decisions, prices, stakes and existing performance populations were identical to the baseline. These are appearances, not independent bets or events.

The real Twins–Giants Dimers panel supplied 51.5% away and 48.5% home, with its probability-specific September 21 13:27 EDT update. Both explicitly displayed values were recorded; neither was inferred as a complement. A current probable-pitcher check supported the named-starter projection. The page's different, untimestamped narrative values were disclosed rather than averaged into the dated panel.

The capture passed exact eligibility and attached to the correct draft cards in a non-published forward-time replay. This used historical execution quotes solely to verify the connection. Both forecasts opposed those historical prices; no new BET or historical revision resulted. The preserved records require a fresh applicability review before another report uses them.

Files: research/staging/2026-09-21-forecast-recovery.json and its recovery-validation companion. No whole-slate availability or automatic retrieval service is claimed.

Validation: 48 targeted tests passed across attribution, forecast import/coverage, event planning, candidate assessment, draft preparation, publication, quote clocks and Graham handoff. The real historical event-plan integration passed. All 12 existing approved replacement-model checks passed. The publishing workflow independently rebuilds and verifies the downstream index against its latest checkout before committing that one output.

## Graham review outcome

The two NYG–LAR spread handoffs were reviewed with current official status and role sources. All four handoff limitations have explicit treatment in research/staging/2026-09-21-graham-handoff-review.json. This is Main-side research with marketViewed:true, not a market-isolated scheduled rating sweep, and it has no Graham write authority.

The source review is complete; numerical adoption is not. The dated fair remains context because current-week replacement/baseline duties and paired historical weekly coverage are unresolved, and no supported native-fair range was established. The already-approved historical equal-share/weighted replacement convention remains in place. Unequal values alone are not a new blocker; that historical adapter does not automatically authorize current-week personnel writes.

No arbitrary player average, uncertainty interval, forecast time or PASS was entered to clear a gate. A blocked receipt without a final decision cannot be fixed by fabricating a final disposition. The next producer must revalidate sources against the new quote-bound handoff and continue the specific remaining research. This release does not claim those material inputs were resolved.
